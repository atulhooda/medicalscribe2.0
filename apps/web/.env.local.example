# Rename this file to .env.local and fill in your secrets.

# Google Gemini API key for clinical note generation (required)
# Get your key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-key"

# Sarvam API key for speech-to-text transcription (required)
# Get your key at: https://dashboard.sarvam.ai/
SARVAM_API_KEY="your-sarvam-key"

# Transcription provider for LIVE segments during recording (low latency):
# - sarvam (default, Sarvam synchronous STT)
# - whisper_local (local tiny Whisper server, desktop only)
# - medasr (legacy local MedASR server)
TRANSCRIPTION_PROVIDER="sarvam"

# Sarvam sync config (used when TRANSCRIPTION_PROVIDER=sarvam)
SARVAM_MODEL="saarika:v2.5"
# Language code: "unknown" auto-detects; or an ISO code like "en-IN", "hi-IN".
SARVAM_LANGUAGE="unknown"

# Authoritative full-recording transcription (feeds the medical note).
# Sarvam Batch STT is asynchronous, has NO 30-second limit, and supports
# native speaker diarization. Set to "sarvam" to force the sync path instead.
FINAL_TRANSCRIPTION_PROVIDER="sarvam_batch"
SARVAM_BATCH_MODEL="saaras:v3"
# saaras:v3 modes: transcribe | translate | verbatim | translit | codemix
SARVAM_BATCH_MODE="transcribe"
SARVAM_BATCH_LANGUAGE="unknown"
SARVAM_BATCH_DIARIZATION="true"
SARVAM_BATCH_NUM_SPEAKERS="2"
SARVAM_BATCH_POLL_INTERVAL_SECONDS="5"
SARVAM_BATCH_TIMEOUT_SECONDS="600"

# Gemini request timeout in milliseconds (optional)
GEMINI_TIMEOUT_MS="45000"

# Local Whisper server config (used only when TRANSCRIPTION_PROVIDER=whisper_local)
WHISPER_LOCAL_URL="http://127.0.0.1:8002/v1/audio/transcriptions"
WHISPER_LOCAL_MODEL="tiny.en"
WHISPER_LOCAL_BACKEND="cpp"
WHISPER_LOCAL_GPU="1"
WHISPER_LOCAL_TIMEOUT_MS="15000"
WHISPER_LOCAL_MAX_RETRIES="2"

# Base64-encoded 32-byte secret used for client-side secure storage.
# Generate with: openssl rand -base64 32
NEXT_PUBLIC_SECURE_STORAGE_KEY="base64-secret"
